{
  "private": true,
  "scripts": {
    "api:dev": "wrangler dev",
    "api:deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply subscribers --local",
    "db:migrate:remote": "wrangler d1 migrations apply subscribers --remote"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
